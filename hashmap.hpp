#include <unordered_map>

class hashmap
{
private:
    std::unordered_map<int,int> um;
public:
    hashmap(/* args */) = default;
    ~hashmap() = delete;

    void insert_perf() {
        um.
    }


};

hashmap::hashmap(/* args */)
{
}

hashmap::~hashmap()
{
}
